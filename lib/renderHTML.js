const path = require("path");
const fs = require("fs");

const templateFolder = path.resolve(__dirname, "templates");

const getRole = teammembers => {

}


//individual functions for: renderManager, renderEngineer, renderIntern 
//connect those functions to template HTML files()
